# DOOM music runtime data

`tools/PrepareThirdParty.ps1` installs `TimGM6mb.sf2` here.

The SoundFont supplies General MIDI instruments for the original MUS music stored in `doom1.wad`. Players do not need to add any audio files themselves.
